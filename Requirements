Flask==2.3.2
gunicorn==21.2.0

services:
  - type: web
    name: clip-affiliate-app
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
